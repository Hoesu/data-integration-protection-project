import numpy as np
import pandas as pd


def compute_interrow_dist(
    x: dict,
    y: dict,
    properties: dict,
) -> float:
    """
    두 행 간의 거리를 계산합니다.
    """
    pass

def compute_pairwise_dist(
    data: pd.DataFrame,
    properties: dict,
) -> np.ndarray[float]:
    """
    전체 데이터프레임에서 모든 행 쌍에 대한 거리 행렬을 계산합니다.
    """
    pass